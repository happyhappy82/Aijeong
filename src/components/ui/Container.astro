---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
  as?: string;
}

const { class: className, as: Component = "div" } = Astro.props;
---

<Component class={cn("mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8", className)}>
  <slot />
</Component>
